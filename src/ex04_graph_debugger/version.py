"""Single source of truth for the package version.

Versioning starts at 1.00 per the course guideline. Keep this string in sync with
the `version` field in pyproject.toml (guarded by tests/unit/test_version.py).
"""

__version__ = "1.00"
